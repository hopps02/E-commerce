enum SlideDirection {
  up,
  down,
  left,
  right,
}

enum RotationDirection {
  left,
  right,
}

enum AnimationType {
  slide,
  fade,
  scale,
  pulse,
  rotation,
  shader,
}

enum ShaderRevealDirection {
  bottomToTop,
  topToBottom,
  leftToRight,
  rightToLeft,
  startToEnd,
  endToStart,
}
