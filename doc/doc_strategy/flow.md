# 🔄 [Feature Name] Flow

---

## 👤 User Flow

1. User opens `[Screen Name]`
2. User enters `[input]`
3. Validation is performed
4. API request is triggered
5. Result:

   * ✅ Success → [what happens]
   * ❌ Failure → [error handling]

---

## 🔁 System Flow

UI → Notifier → UseCase → Repository → API → Response → UI

---

## ⏱️ Special Logic (if any)

* Timer (e.g. OTP countdown)
* Retry logic
* Debounce
* Pagination

---

## ⚠️ Edge Cases

* [ ] Invalid input
* [ ] Network failure
* [ ] Timeout
* [ ] Server error
* [ ] Empty state

---

## 📍 Navigation

* From → `[Screen A]`
* To → `[Screen B]`
* Conditions → [when navigation happens]
