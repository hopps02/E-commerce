app_identifier "com.utr.store" # iOS bundle id

# Apple account that owns the app (Admin / App Manager on UTR's account).
# Fill these with UTR's real Apple Developer values before uploading:
apple_id ENV["APPLE_ID"]              # e.g. "dev@utr.sa"
itc_team_id ENV["ITC_TEAM_ID"]        # App Store Connect team id (Users and Access > Membership)
team_id ENV["DEV_TEAM_ID"]            # Apple Developer (signing) team id, e.g. "X12345678"
